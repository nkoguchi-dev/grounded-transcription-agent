# Grounded Transcription Agent

## 対象範囲

Phase 1 では、ローカルで動く非同期ジョブ基盤を構築する。対応する Phase が開始されるまで、
フロントエンド、MCP サーバー、認証、文字起こしプロバイダーとの連携を追加しない。

## 開発時の共通ルール

- PR を作成する前に、base が想定したリモート既定ブランチであること、および比較差分が対象 Issue の
  変更だけであることを確認する。無関係な変更や未公開のローカルコミットは別 PR に分離する。

## 作業ブランチ

- 作業を開始する前にリモートの最新 `main` を取得・確認する。
- 最新の `origin/main` のコミットを起点に、対象 Issue 専用の作業ブランチを作成して作業する。
- `main` ブランチへ直接コミットしない。
- 既存の作業ブランチを使い回す場合も、開始時点の base と、差分が対象 Issue の変更だけであることを
  確認する。

## PR 作成後の自己レビュー

- PR を作成したら、`docs/REVIEW_GUIDE.md` の該当する観点を使って自己レビューする。
- 自己レビューの結果を PR のトップレベルコメントとして追加する。
- コメントには、少なくとも次を記載する。
  - 確認した観点と、各観点の結果（問題なし／対応済み／該当なし）。
  - 実行した品質チェックとその結果。
  - 未解消の指摘がある場合は、その理由と後続対応。指摘を解消した場合は、結果を更新する。

## Issue と PR の連携

- PR の説明には、対応する Issue を明記する。
- PR により Issue を完了させる場合は、単なる URL や `#<Issue番号>` の参照ではなく、
  `Closes #<Issue番号>`、`Fixes #<Issue番号>`、`Resolves #<Issue番号>` などの closing keyword を使う。
- closing keyword による自動クローズは、PR の base が既定ブランチ（現在は `main`）であり、その PR が
  マージされた場合に限られることを前提にする。
- Issue の一部だけを扱う PR は closing keyword を使わず、`Refs #<Issue番号>` などの参照にとどめる。

## 作業完了時の Issue 記録

- PR をレビュー可能な状態にした時点、または PR を作らない作業を完了した時点で、対象 Issue の
  トップレベルコメントとして実施内容を記録する。
- コメントには、変更・判断の要約、実行した検証と結果、残る作業・制約があればその内容を記載する。
- PR がある場合はそのリンクも記載し、Issue と成果物の対応を追跡可能にする。
